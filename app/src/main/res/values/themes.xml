<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.PayCard" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/text_dark</item>
        <item name="colorOnSurfaceVariant">@color/text_hint</item>
        <item name="colorOutline">@color/outline</item>
        <item name="colorNameBar">@color/name_bar</item>
        <item name="android:windowBackground">@color/screen_bg</item>
        <item name="android:colorBackground">@color/screen_bg</item>
        <item name="materialAlertDialogTheme">@style/ThemeOverlay.PayCard.Dialog</item>
        <item name="android:windowAnimationStyle">@style/Animation.PayCard.Activity</item>
    </style>

    <!-- 付款页跟随主题：浅色白底、深色深底 -->
    <style name="Theme.PayCard.Pay" parent="Theme.PayCard">
        <item name="android:windowBackground">@color/surface</item>
    </style>

    <!-- 统一弹窗外观：圆角、主题色按钮 -->
    <style name="ThemeOverlay.PayCard.Dialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="dialogCornerRadius">24dp</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/text_dark</item>
    </style>

    <!-- 应用内自定义 Dialog：透明底、居中缩放淡入 -->
    <style name="Dialog.PayCard" parent="Theme.Material3.DayNight.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimAmount">0.45</item>
        <item name="android:windowAnimationStyle">@style/Animation.PayCard.Dialog</item>
    </style>

    <style name="Animation.PayCard.Dialog" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/dialog_exit</item>
    </style>

    <!-- 页面切换：右进左出淡入淡出 -->
    <style name="Animation.PayCard.Activity" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_right</item>
        <item name="android:activityOpenExitAnimation">@anim/fade_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/fade_in</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_out_right</item>
    </style>

    <!-- 莫兰迪配色叠加：仅覆盖主色四项，切换后 recreate 生效 -->
    <style name="ThemeOverlay.PayCard.GrayGreen" parent="">
        <item name="colorPrimary">@color/gg_primary</item>
        <item name="colorPrimaryDark">@color/gg_dark</item>
        <item name="colorSecondary">@color/gg_accent</item>
        <item name="colorNameBar">@color/gg_bar</item>
    </style>

    <style name="ThemeOverlay.PayCard.GrayBlue" parent="">
        <item name="colorPrimary">@color/gb_primary</item>
        <item name="colorPrimaryDark">@color/gb_dark</item>
        <item name="colorSecondary">@color/gb_accent</item>
        <item name="colorNameBar">@color/gb_bar</item>
    </style>

    <style name="ThemeOverlay.PayCard.LotusPink" parent="">
        <item name="colorPrimary">@color/lp_primary</item>
        <item name="colorPrimaryDark">@color/lp_dark</item>
        <item name="colorSecondary">@color/lp_accent</item>
        <item name="colorNameBar">@color/lp_bar</item>
    </style>

    <style name="ThemeOverlay.PayCard.Terracotta" parent="">
        <item name="colorPrimary">@color/tc_primary</item>
        <item name="colorPrimaryDark">@color/tc_dark</item>
        <item name="colorSecondary">@color/tc_accent</item>
        <item name="colorNameBar">@color/tc_bar</item>
    </style>

    <style name="ThemeOverlay.PayCard.GrayMauve" parent="">
        <item name="colorPrimary">@color/gm_primary</item>
        <item name="colorPrimaryDark">@color/gm_dark</item>
        <item name="colorSecondary">@color/gm_accent</item>
        <item name="colorNameBar">@color/gm_bar</item>
    </style>
</resources>
